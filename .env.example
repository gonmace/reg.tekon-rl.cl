# ── Django ───────────────────────────────────────────────────────────────────
SECRET_KEY=cambia-esto-a-una-clave-secreta-larga-y-aleatoria
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
ADMIN_URL=admin/
SITE_URL=http://localhost:8000

# ── Base de datos (SQLite por defecto si no se define POSTGRES_DB) ────────────
# POSTGRES_DB=reportestekon
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=password
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432

# ── Redis (cache y sesiones) ──────────────────────────────────────────────────
REDIS_URL=redis://localhost:6379/0

# ── Email (consola por defecto si no se define EMAIL_HOST) ───────────────────
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_USE_SSL=False
# EMAIL_HOST_USER=usuario@gmail.com
# EMAIL_HOST_PASSWORD=contraseña
# DEFAULT_FROM_EMAIL=noreply@example.com

# ── CSRF (producción) ─────────────────────────────────────────────────────────
# CSRF_TRUSTED_ORIGINS=https://tu-dominio.com,https://www.tu-dominio.com
